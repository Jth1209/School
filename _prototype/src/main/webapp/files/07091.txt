변수의 바이트 기본적인 상식

클래스 변수 작성 방식 정하기
primitive(원시적): 메모리가 바로 인식한다.   non primitive(비원시적)

" = " 오른쪽 데이터를 왼쪽 메모리에 삽입 (포인터 embedded)
String 기본적으로 가변 메모리를 사용(어느정도는 가능)
byte 형은 127까지 선언 가능 p82

long a = 20; a가 8바이트로 메모리 잡음
20 = int -> long형으로 변경(literal= int/ double)
20 = long
자바에서도 printf(" ", args)쓸수 있음
print format= printf

다형성 (polymorphism)
상속 (inheritation)
캡슐화 (incapsulation)

public static void method(para, para2){ }

long 변수에 L붙이는 이유
unicode사용한다

/, \ slash back slash
0,1
true,false
+,-
